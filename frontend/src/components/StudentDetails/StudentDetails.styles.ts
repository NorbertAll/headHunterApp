import { Container, styled } from '@mui/material';
import { pxToRem } from '../../utils/styles';

export const StyledContainerStudentDetails = styled(Container)(() => ({
  margin: `${pxToRem(26)} auto`,
}));
